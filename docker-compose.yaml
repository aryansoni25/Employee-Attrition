version: "3.9"

services:

  backend:
    build: ./backend
    container_name: attrition_backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: attrition_frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend